// singleton -> when we constucker from constructor
//Object.create

// object literals
const Jsuswe={
    name:"hi" // key , values by default string "name" 
}

Jsuswe.name // to access
console.log(Jsuswe["name"]); // to acesss object